# Core
pydantic>=2.0,<3.0
pyyaml>=6.0

# Dashboard
streamlit>=1.35.0
plotly>=5.22.0
pandas>=2.2.0

# YouTube ingestion (real API)
# google-api-python-client>=2.130.0   # TODO: uncomment to enable real YouTube API calls
# youtube-transcript-api>=0.6.2       # TODO: uncomment to enable real transcript fetching

# LLM – Ollama (local inference)
ollama>=0.4.0                          # pip install ollama  (requires Ollama server running locally)

# Dev / testing
pytest>=8.0
pytest-cov>=5.0
